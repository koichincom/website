---
import logoSvg from "../assets/logo.svg?raw";

export interface Props {
    className?: string;
}
const { className } = Astro.props;
const svgMarkup = logoSvg.replace(
    "<svg",
    `<svg class="${className ?? ""}"`
);
---
<span set:html={svgMarkup} />
