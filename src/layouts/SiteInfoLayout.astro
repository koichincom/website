---
import { formatDate } from "../utils/date-formatter";
import NewTabLink from "../components/NewTabLink.astro";
import ProseLayout from "./ProseLayout.astro";

export interface Props {
    title: string;
    effectiveDate?: Date;
}
const { title, effectiveDate } = Astro.props;
const website = "https://koichin.com";
const owner = "Koichi Nakayamada";
const formattedEffectiveDate = effectiveDate ? formatDate(effectiveDate) : "";
const contact = "mailto:k@koichin.com";
---

<ProseLayout title={title}>
    <p>
        Effective date: {formattedEffectiveDate}<br />
        Website: <NewTabLink href={website}>{website}</NewTabLink><br />
        Owner: {owner}<br />
        Contact: <NewTabLink href={contact}>{contact.replace("mailto:", "")}</NewTabLink>
    </p>
    <slot />
</ProseLayout>
