---
export interface Props {
    href: string;
    class?: string;
    ariaLabel?: string;
    target?: string;
    rel?: string;
    [key: string]: any;
}

const {
    href,
    class: className = "",
    ariaLabel,
    target,
    rel,
    ...rest
} = Astro.props;
---

<a
    href={href}
    class={className}
    target={target}
    rel={rel}
    aria-label={ariaLabel}
    {...rest}
>
    <span class="inline-flex items-baseline">
        <slot /><span
            class="ml-[0.15em] inline-flex items-center self-center"
            aria-hidden="true"
        >
            <slot name="icon" />
        </span>
    </span>
</a>
