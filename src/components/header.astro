---
const currentPath = Astro.url.pathname;
const isHome = currentPath === "/";
const isBlog = currentPath === "/blog";
const isBlogPost = currentPath.startsWith("/blog/");
const isProjects = currentPath.startsWith("/proj");
const isClub = currentPath.startsWith("/club");
const isVision = currentPath.startsWith("/vision");
---

<nav
    class="mb-8 flex flex-wrap gap-4 sm:gap-6 text-base text-(--color-text-second) dark:text-(--color-text-second-dark)"
>
    {
        isHome ? (
            <span class="text-(--color-accent-light) dark:text-(--color-accent-dark)">
                Home
            </span>
        ) : (
            <span class="relative group hidden sm:inline-block">
                <a
                    href="/"
                    class="hover:text-(--color-accent-light) dark:hover:text-(--color-accent-dark) transition-colors duration-200"
                >
                    Home
                </a>
                <span class="absolute bottom-full left-1/2 -translate-x-1/2 mb-2 px-2 py-1 text-xs bg-(--color-text-first) dark:bg-(--color-text-first-dark) text-(--color-background) dark:text-(--color-background-dark) rounded opacity-0 group-hover:opacity-100 transition-opacity duration-200 pointer-events-none whitespace-nowrap">
                    Press h
                </span>
            </span>
            <a
                href="/"
                class="hover:text-(--color-accent-light) dark:hover:text-(--color-accent-dark) transition-colors duration-200 sm:hidden"
            >
                Home
            </a>
        )
    }
    {
        isBlog ? (
            <span class="text-(--color-accent-light) dark:text-(--color-accent-dark)">
                Blog
            </span>
        ) : (
            isBlogPost ? (
                <a
                    href="/blog"
                    class="text-(--color-accent-light) dark:text-(--color-accent-dark)"
                >
                    Blog
                </a>
            ) : (
                <>
                    <span class="relative group hidden sm:inline-block">
                        <a
                            href="/blog"
                            class="hover:text-(--color-accent-light) dark:hover:text-(--color-accent-dark) transition-colors duration-200"
                        >
                            Blog
                        </a>
                        <span class="absolute bottom-full left-1/2 -translate-x-1/2 mb-2 px-2 py-1 text-xs bg-(--color-text-first) dark:bg-(--color-text-first-dark) text-(--color-background) dark:text-(--color-background-dark) rounded opacity-0 group-hover:opacity-100 transition-opacity duration-200 pointer-events-none whitespace-nowrap">
                            Press b
                        </span>
                    </span>
                    <a
                        href="/blog"
                        class="hover:text-(--color-accent-light) dark:hover:text-(--color-accent-dark) transition-colors duration-200 sm:hidden"
                    >
                        Blog
                    </a>
                </>
            )
        )
    }
    {
        isProjects ? (
            <span class="text-(--color-accent-light) dark:text-(--color-accent-dark)">
                Projects
            </span>
        ) : (
            <>
                <span class="relative group hidden sm:inline-block">
                    <a
                        href="/proj"
                        class="hover:text-(--color-accent-light) dark:hover:text-(--color-accent-dark) transition-colors duration-200"
                    >
                        Projects
                    </a>
                    <span class="absolute bottom-full left-1/2 -translate-x-1/2 mb-2 px-2 py-1 text-xs bg-(--color-text-first) dark:bg-(--color-text-first-dark) text-(--color-background) dark:text-(--color-background-dark) rounded opacity-0 group-hover:opacity-100 transition-opacity duration-200 pointer-events-none whitespace-nowrap">
                        Press p
                    </span>
                </span>
                <a
                    href="/proj"
                    class="hover:text-(--color-accent-light) dark:hover:text-(--color-accent-dark) transition-colors duration-200 sm:hidden"
                >
                    Projects
                </a>
            </>
        )
    }
    {
        isClub && (
            <span class="text-(--color-accent-light) dark:text-(--color-accent-dark)">
                Club
            </span>
        )
    }
    {
        isVision && (
            <span class="text-(--color-accent-light) dark:text-(--color-accent-dark)">
                Vision
            </span>
        )
    }
</nav>
